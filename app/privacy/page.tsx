import { PrivacyScreen } from '@/components/PrivacyScreen';

export const metadata = {
  title: 'El Carot — Privacidad',
  description: 'El Carot no recopila, no transmite y no comparte datos personales. Todo se queda en tu dispositivo.',
};

export default function PrivacyPage() {
  return <PrivacyScreen />;
}
